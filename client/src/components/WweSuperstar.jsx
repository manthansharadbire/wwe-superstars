import React from 'react'

function WweSuperstar({
    _id, wwename, height, finisher, aka, thumbnail
}) {
  return (
    <div>
  <h2>{wwename}</h2>
  <h2>{height}</h2>
    </div>
  )
}

export default WweSuperstar;
